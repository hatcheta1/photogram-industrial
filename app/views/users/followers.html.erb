<%= render "users/tabs", user: current_user %>

<h1>
  Followers
</h1>

<div class="row">
  <div class="col-md-6 offset-md-3">
    <ul class="list-group">
      <% @user.accepted_received_follow_requests.each do |follow_request| %>
        <li class="list-group-item"><%= follow_request.sender.username %></li>
      <% end %>
    </ul>
  </div>
</div>
